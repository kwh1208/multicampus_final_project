<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eat.table.dao.ReviewDAO">
	<select id="ReviewSelect" resultType="com.eat.table.dto.ReviewDTO">
	 	select s.store_name, r.review_time, r.score, r.comment_time
		from review r
		join store s on s.seq=r.store_seq
		where r.user_id=#{param1}
	</select>
</mapper>