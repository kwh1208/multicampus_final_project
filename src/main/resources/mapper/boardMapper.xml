<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="eat_schedule.dao.BoardDAO">
	<insert id="inquiryInsert" parameterType="eat_schedule.dto.BoardDTO">
		insert into qna(user_id, question_title, question, question_time)
		values(#{user_id},  #{question_title}, #{question}, NOW())
	</insert>
</mapper>